# GardenGong
A vibraphone simulator. Displays a set of adjustable bars which play a note, the note having frequency proportional to their length.

To run it, clone the repo and use ```npm install``` and then ```npm run dev```.
